---
import Layout from '../layouts/Layout.astro';
---

<Layout
	title="Relic芯片 | 赛博朋克2077 - 人格意识体生物芯片"
	description="Relic生物芯片详解 - Soulcore 1.0技术规格、工作原理、版本对比及风险警告。荒坂公司最新意识移植技术，实现数字永生。"
>
	<!-- 页面标题 -->
	<section class="page-header">
		<div class="container">
			<p class="page-tag">// 荒坂公司绝密档案</p>
			<h1 class="page-title glitch" data-text="RELIC芯片">RELIC芯片</h1>
			<p class="page-subtitle">人格意识体生物芯片 · Soulcore 1.0</p>
		</div>
	</section>

	<!-- 芯片介绍 -->
	<section class="section">
		<div class="container">
			<div class="content-grid">
				<div class="content-text">
					<h2 class="content-title">什么是 Relic</h2>
					<p>
						Relic，全称人格意识体生物芯片，是荒坂公司旗舰级实验性项目"守护你的灵魂"的核心产品。
						它基于人格转移技术，将人类神经系统中的数据转化为数字化印迹（Engram），
						并将其作为意识体储存在生物芯片上。
					</p>
					<p>
						设计初衷听起来十分温情——让你在亲人去世后还能和他对话，
						就像电子宠物一样留住逝者的声音和记忆。
						但这项技术的真正潜力，远不止于此。
					</p>
					<div class="highlight-box">
						<span class="highlight-label">官方宣传语</span>
						<p class="highlight-text">"守护你的灵魂 · Secure Your Soul"</p>
					</div>
				</div>
				<div class="content-visual">
					<div class="chip-diagram">
						<div class="chip-main">
							<span class="chip-label">SOULCORE</span>
							<span class="chip-version">v1.0</span>
						</div>
						<div class="chip-parts">
							<div class="part">
								<span class="part-dot"></span>
								<span class="part-name">神经接口</span>
							</div>
							<div class="part">
								<span class="part-dot"></span>
								<span class="part-name">生物存储</span>
							</div>
							<div class="part">
								<span class="part-dot"></span>
								<span class="part-name">意识处理器</span>
							</div>
							<div class="part">
								<span class="part-dot"></span>
								<span class="part-name">DNA改写模块</span>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- 技术版本 -->
	<section class="section alt-bg">
		<div class="container">
			<div class="section-header">
				<h2 class="section-title glitch" data-text="技术版本">技术版本</h2>
				<p class="section-subtitle">// Relic 芯片的迭代历程</p>
			</div>
			<div class="version-grid">
				<div class="version-card">
					<div class="version-badge">1.0</div>
					<h3 class="version-title">Relic 初代</h3>
					<p class="version-desc">
						仅用于与预先保存的意识体进行交流，
						意识体带有人为设置的限制，
						无法完全复刻原始人格。
					</p>
					<ul class="version-features">
						<li>意识读取功能</li>
						<li>基础交互能力</li>
						<li>仅限富豪客户</li>
					</ul>
				</div>
				<div class="version-card featured">
					<div class="version-badge">2.0</div>
					<div class="featured-tag">绝密</div>
					<h3 class="version-title">Relic 2.0</h3>
					<p class="version-desc">
						由荒坂三郎亲自监督的顶级机密项目，
						包含完整意识体和自我意识人格构造，
						可植入新的有机宿主并激活。
					</p>
					<ul class="version-features">
						<li>完整意识移植</li>
						<li>DNA改写功能</li>
						<li>神经通路覆盖</li>
						<li>人格覆盖能力</li>
					</ul>
				</div>
			</div>
		</div>
	</section>

	<!-- 工作原理 -->
	<section class="section">
		<div class="container">
			<div class="section-header">
				<h2 class="section-title glitch" data-text="工作原理">工作原理</h2>
				<p class="section-subtitle">// 意识移植的技术流程</p>
			</div>
			<div class="process-steps">
				<div class="step">
					<div class="step-number">01</div>
					<h3 class="step-title">意识扫描</h3>
					<p class="step-desc">
						通过灵魂杀手（Soulkiller）技术将人类意识
						从生物大脑中提取并转化为数字印迹（Engram）。
					</p>
				</div>
				<div class="step">
					<div class="step-number">02</div>
					<h3 class="step-title">芯片存储</h3>
					<p class="step-desc">
						将数字化的意识体存储在Relic生物芯片中，
						芯片可在常温下永久保存意识数据。
					</p>
				</div>
				<div class="step">
					<div class="step-number">03</div>
					<h3 class="step-title">神经植入</h3>
					<p class="step-desc">
						通过神经接口将芯片植入宿主体内，
						芯片开始与生物大脑建立连接。
					</p>
				</div>
				<div class="step">
					<div class="step-number">04</div>
					<h3 class="step-title">意识覆盖</h3>
					<p class="step-desc">
						芯片逐步改写宿主的神经通路和DNA，
						最终实现意识体对宿主身体的完全接管。
					</p>
				</div>
			</div>
		</div>
	</section>

	<!-- 风险警告 -->
	<section class="section warning-section">
		<div class="container">
			<div class="warning-box">
				<div class="warning-icon">⚠</div>
				<h2 class="warning-title">风险警告</h2>
				<div class="warning-content">
					<div class="warning-item">
						<span class="warning-label">人格覆盖</span>
						<p>芯片会逐渐改写宿主的神经通路，最终导致原有人格被完全覆盖</p>
					</div>
					<div class="warning-item">
						<span class="warning-label">DNA变异</span>
						<p>长期植入可能导致宿主DNA发生不可逆的变异</p>
					</div>
					<div class="warning-item">
						<span class="warning-label">身体排斥</span>
						<p>部分宿主可能出现身体排斥反应，导致器官衰竭</p>
					</div>
					<div class="warning-item">
						<span class="warning-label">意识冲突</span>
						<p>宿主意识与芯片意识可能产生冲突，导致精神分裂</p>
					</div>
				</div>
				<p class="warning-disclaimer">
					* 以上信息来自荒坂公司内部机密文档泄露，仅供研究参考。
					意识移植技术仍处于实验阶段，风险未知。
				</p>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* 页面标题 */
	.page-header {
		padding: 120px 0 80px;
		text-align: center;
		position: relative;
	}

	.page-tag {
		color: var(--primary-red);
		font-size: 12px;
		letter-spacing: 3px;
		margin-bottom: 20px;
		text-transform: uppercase;
	}

	.page-title {
		font-size: 64px;
		font-weight: bold;
		color: var(--primary-yellow);
		letter-spacing: 10px;
		margin-bottom: 15px;
	}

	.page-subtitle {
		color: var(--text-secondary);
		font-size: 18px;
		letter-spacing: 3px;
	}

	/* 内容网格 */
	.content-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 60px;
		align-items: center;
	}

	.content-title {
		font-size: 28px;
		color: var(--primary-yellow);
		margin-bottom: 25px;
		letter-spacing: 2px;
	}

	.content-text p {
		color: var(--text-secondary);
		font-size: 15px;
		line-height: 1.8;
		margin-bottom: 20px;
	}

	.highlight-box {
		margin-top: 30px;
		padding: 25px;
		border-left: 3px solid var(--primary-yellow);
		background: rgba(238, 229, 0, 0.05);
	}

	.highlight-label {
		display: block;
		font-size: 12px;
		color: var(--primary-yellow);
		letter-spacing: 2px;
		margin-bottom: 10px;
		text-transform: uppercase;
	}

	.highlight-text {
		font-size: 18px;
		color: var(--text-primary);
		font-style: italic;
	}

	/* 芯片图示 */
	.chip-diagram {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 30px;
	}

	.chip-main {
		width: 180px;
		height: 250px;
		background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
		border: 2px solid var(--primary-yellow);
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		position: relative;
		animation: chip-pulse 2s ease-in-out infinite;
	}

	.chip-main::before,
	.chip-main::after {
		content: '';
		position: absolute;
		width: 15px;
		height: 25px;
		background: var(--dark-tertiary);
		border: 1px solid var(--primary-yellow);
	}

	.chip-main::before {
		left: -20px;
		top: 50%;
		transform: translateY(-50%);
	}

	.chip-main::after {
		right: -20px;
		top: 50%;
		transform: translateY(-50%);
	}

	.chip-label {
		font-size: 20px;
		font-weight: bold;
		color: var(--primary-yellow);
		letter-spacing: 4px;
	}

	.chip-version {
		font-size: 12px;
		color: var(--text-secondary);
		margin-top: 8px;
	}

	@keyframes chip-pulse {
		0%, 100% { box-shadow: 0 0 20px rgba(238, 229, 0, 0.2); }
		50% { box-shadow: 0 0 40px rgba(238, 229, 0, 0.4); }
	}

	.chip-parts {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 15px;
		width: 100%;
		max-width: 300px;
	}

	.part {
		display: flex;
		align-items: center;
		gap: 10px;
		padding: 12px;
		background: rgba(26, 26, 26, 0.8);
		border: 1px solid var(--dark-tertiary);
	}

	.part-dot {
		width: 8px;
		height: 8px;
		border-radius: 50%;
		background: var(--primary-yellow);
		flex-shrink: 0;
	}

	.part-name {
		font-size: 12px;
		color: var(--text-secondary);
	}

	/* 版本卡片 */
	.version-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 40px;
		max-width: 800px;
		margin: 0 auto;
	}

	.version-card {
		padding: 40px;
		background: rgba(26, 26, 26, 0.8);
		border: 1px solid var(--dark-tertiary);
		position: relative;
		transition: all 0.3s ease;
	}

	.version-card:hover {
		border-color: var(--primary-yellow);
		transform: translateY(-5px);
	}

	.version-card.featured {
		border-color: var(--primary-red);
		background: rgba(214, 25, 45, 0.05);
	}

	.featured-tag {
		position: absolute;
		top: 20px;
		right: 20px;
		padding: 5px 12px;
		background: var(--primary-red);
		color: white;
		font-size: 10px;
		letter-spacing: 2px;
		text-transform: uppercase;
	}

	.version-badge {
		display: inline-block;
		font-size: 48px;
		font-weight: bold;
		color: var(--primary-yellow);
		margin-bottom: 20px;
		font-family: 'Courier New', monospace;
	}

	.version-title {
		font-size: 22px;
		color: var(--text-primary);
		margin-bottom: 15px;
		letter-spacing: 2px;
	}

	.version-desc {
		color: var(--text-secondary);
		font-size: 14px;
		line-height: 1.8;
		margin-bottom: 20px;
	}

	.version-features {
		list-style: none;
	}

	.version-features li {
		color: var(--text-secondary);
		font-size: 13px;
		padding: 8px 0;
		padding-left: 20px;
		position: relative;
	}

	.version-features li::before {
		content: '▸';
		position: absolute;
		left: 0;
		color: var(--primary-yellow);
	}

	/* 流程步骤 */
	.process-steps {
		display: grid;
		grid-template-columns: repeat(4, 1fr);
		gap: 30px;
	}

	.step {
		text-align: center;
		padding: 30px 20px;
		background: rgba(26, 26, 26, 0.8);
		border: 1px solid var(--dark-tertiary);
		position: relative;
	}

	.step::after {
		content: '→';
		position: absolute;
		right: -20px;
		top: 50%;
		transform: translateY(-50%);
		color: var(--primary-yellow);
		font-size: 24px;
	}

	.step:last-child::after {
		display: none;
	}

	.step-number {
		font-size: 48px;
		font-weight: bold;
		color: var(--primary-yellow);
		opacity: 0.3;
		margin-bottom: 15px;
		font-family: 'Courier New', monospace;
	}

	.step-title {
		font-size: 18px;
		color: var(--text-primary);
		margin-bottom: 10px;
		letter-spacing: 1px;
	}

	.step-desc {
		color: var(--text-secondary);
		font-size: 13px;
		line-height: 1.6;
	}

	/* 警告区域 */
	.warning-section {
		background: linear-gradient(180deg, transparent 0%, rgba(214, 25, 45, 0.05) 100%);
	}

	.warning-box {
		max-width: 900px;
		margin: 0 auto;
		padding: 50px;
		border: 2px solid var(--primary-red);
		background: rgba(214, 25, 45, 0.05);
		text-align: center;
	}

	.warning-icon {
		font-size: 60px;
		color: var(--primary-red);
		margin-bottom: 20px;
	}

	.warning-title {
		font-size: 32px;
		color: var(--primary-red);
		margin-bottom: 40px;
		letter-spacing: 5px;
		text-transform: uppercase;
	}

	.warning-content {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 30px;
		margin-bottom: 30px;
		text-align: left;
	}

	.warning-item {
		padding: 20px;
		background: rgba(0, 0, 0, 0.3);
		border-left: 3px solid var(--primary-red);
	}

	.warning-label {
		display: block;
		font-size: 14px;
		color: var(--primary-red);
		font-weight: bold;
		margin-bottom: 8px;
		letter-spacing: 1px;
	}

	.warning-item p {
		color: var(--text-secondary);
		font-size: 13px;
		line-height: 1.6;
	}

	.warning-disclaimer {
		color: var(--text-secondary);
		font-size: 12px;
		font-style: italic;
		padding-top: 20px;
		border-top: 1px solid rgba(214, 25, 45, 0.3);
	}

	/* 交替背景 */
	.alt-bg {
		background: rgba(26, 26, 26, 0.3);
	}

	/* 响应式 */
	@media (max-width: 968px) {
		.page-title {
			font-size: 42px;
			letter-spacing: 6px;
		}

		.content-grid {
			grid-template-columns: 1fr;
		}

		.version-grid {
			grid-template-columns: 1fr;
		}

		.process-steps {
			grid-template-columns: repeat(2, 1fr);
		}

		.step::after {
			display: none;
		}

		.warning-content {
			grid-template-columns: 1fr;
		}
	}

	@media (max-width: 576px) {
		.page-title {
			font-size: 32px;
			letter-spacing: 4px;
		}

		.process-steps {
			grid-template-columns: 1fr;
		}

		.warning-box {
			padding: 30px 20px;
		}
	}
</style>
